{
  "name": "@ekx/third-party",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "release": "changeset publish",
    "version": "changeset version && yarn run update-lock",
    "update-lock": "yarn && git add yarn.json",
    "dev:check-ci": "wsrun -m build && wsrun -m recipe:build && wsrun -m package:test",
    "postinstall": "wsrun -m postinstall",
    "ci:build": "wsrun -m -e -t build",
    "ci:build-recipes": "wsrun -m -e -t recipe:build",
    "ci:test-packages": "wsrun -m -t package:test",
    "pm:reset": "rimraf **/node_modules/ **/package-lock.json **/yarn.lock"
  },
  "dependencies": {
    "@changesets/cli": "^2.14.1",
    "wsrun": "^5.2.4"
  },
  "devDependencies": {
    "@ekx/cli-utils": "*",
    "rimraf": "^3.0.2"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
